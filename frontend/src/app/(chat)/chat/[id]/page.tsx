/**
 * Chat session route (/chat/[id]).
 * ChatInterface is rendered by the parent (chat) layout and reads
 * useParams() to get the active session id — loading and displaying messages.
 */
export default function ChatSessionPage() {
  return null;
}
