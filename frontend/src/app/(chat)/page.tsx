/**
 * New-chat route (/).
 * ChatInterface is rendered by the parent (chat) layout and reads
 * useParams() to detect there is no active session — showing the intro screen.
 */
export default function NewChatPage() {
  return null;
}
