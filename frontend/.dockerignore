# Dependencies and build output are recreated inside the image / synced at runtime.
node_modules
.next
out

# Local env and logs
.env*.local
npm-debug.log*
yarn-error.log*

# VCS / tooling
.git
.gitignore
eslint_output.json

# OS
.DS_Store
Thumbs.db
